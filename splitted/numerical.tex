%!TEX root = ../main.tex
\section{A spectral computational route}
\label{sec:numerical}

The blow-up reduction of \S\ref{sub:blowup} does more than clarify the theory: it dictates the \emph{discretisation}.
Because every residual quantity localises to the universal model wedge $\mathcal{L}_W$ of Proposition~\ref{prop:blowup},
governed by $(\nu,\hat K)$ alone, the natural object to compute on is the wedge, not a meshed body. This section records
the resulting numerical route and the validation of its two building blocks. The guiding observation is that the corner
is \emph{scale}-covariant ($r\mapsto cr$), not translation-covariant, so the transform that diagonalises it is the
Mellin transform (Fourier in $\log r$), with a genuine Fourier transform reserved for the smooth edge
(Proposition~\ref{prop:blowup}(iii)). A graded-mesh finite-element discretisation is therefore not required; a
Mellin\,$\times$\,spectral scheme converges exponentially where a mesh converges only algebraically.

\subsection{The corner pencil by a Mellin\,\texorpdfstring{$\times$}{x}\,Chebyshev scheme}
\label{sub:numpencil}

Substituting the separable Mellin ansatz $\bm{u}=r^{\lambda}\big(a(\theta),b(\theta)\big)$ into the plane-strain
Navier--Lam\'e equations on the wedge $\theta\in(0,\tfrac\pi2)$ reduces the corner to an angular two-point eigenvalue
problem --- a quadratic eigenvalue problem in $\lambda$,
\begin{equation}
    \label{eq:qep}
    \big(\lambda^2\,\mathbb{M}_2+\lambda\,\mathbb{M}_1+\mathbb{M}_0\big)\,\big(a,b\big)^{\!\top}=0,
\end{equation}
with the clamped conditions $a=b=0$ at $\theta=0$ and the traction-free conditions $\sigma_{\theta\theta}=\sigma_{r\theta}=0$
at $\theta=\tfrac\pi2$. Discretising the angular operators by Chebyshev collocation and linearising \eqref{eq:qep} to a
generalised eigenvalue problem yields the singular exponents as the eigenvalues with $\Re\lambda\in(0,1)$; the smallest is
the exponent $\alpha(\nu)$ of \eqref{eq:kondratiev} and Figure~\ref{fig:williams}.

This spectral computation agrees with the independent Kolosov--Muskhelishvili secular determinant of
Proposition~\ref{prop:bcount} to ten digits across the physical range (e.g.\ $\alpha(0.3)=0.7111729$ from both), and
converges \emph{exponentially} in the number $N$ of angular modes:
\begin{center}
    \begin{tabular}{rl}
        \toprule
        $N$ & $|\alpha-\alpha_{\mathrm{ref}}|$ at $\nu=0.3$ \\
        \midrule
        $6$  & $1.1\times10^{-4}$ \\
        $8$  & $9.1\times10^{-7}$ \\
        $12$ & $3.6\times10^{-10}$ \\
        \bottomrule
    \end{tabular}
\end{center}
Twelve angular modes already reach machine precision, a saturation no graded mesh attains; this is the concrete sense in
which the corner is better resolved spectrally than by FEM. The computation is reproduced by
\texttt{mellin\_chebyshev\_pencil.py} (validated against \texttt{williams\_clampedfree.py}).

\subsection{Profile nondegeneracy: the hypothesis (W)}
\label{sub:numW}

% The screening lemma's kinematic hypothesis is a one-line check on the computed profile; we run it and report both
% the confirmation and the failure region honestly, since the answer splits the physical nu range in two.
The kinematic hypothesis (W) of Lemma~\ref{lem:screen} is a closed-form condition on the already-computed angular
profile: with $\bm G(\theta)=\alpha\,\bm\Phi\otimes\bm e_r+\bm\Phi'\otimes\bm e_\theta$, it requires
$\mu(\theta)=\det\bm G=\alpha\,\bm\Phi\times\bm\Phi'\ge0$ on $[0,\tfrac\pi2]$ with finite-order zeros and
$\tr\bm G\neq0$ at each zero. Both $\mu$ and the vanishing of $\tr\bm G$ are invariant under the residual scaling of
the mode, so the check is parameter-free. Evaluating it on the Kolosov--Muskhelishvili null mode (analytic
$\theta$-derivatives, verified against finite differences to $10^{-9}$; \texttt{williams\_profile\_W.py}) gives a
sharp answer:
\begin{center}
    \begin{tabular}{ccccc}
        \toprule
        $\nu$ & $\alpha$ & $\min_\theta \mu/\|\bm G\|^2$ & interior zero $\theta_z/(\pi/2)$ & verdict \\
        \midrule
        $0.10$   & $0.86704$ & $-1.9\times10^{-2}$ & $0.975$ & fails \\
        $0.25$   & $0.74475$ & $-3.9\times10^{-3}$ & $0.470$ & fails \\
        $0.35$   & $0.67970$ & $-2.3\times10^{-4}$ & $0.173$ & fails \\
        $0.40$   & $0.64991$ & $-1.3\times10^{-7}$ & $0.014$ & fails (marginally) \\
        $0.4042$ & $0.64748$ & $-1\times10^{-14}$  & ---     & threshold $\nu_W$ \\
        $0.417$  & $0.64012$ & $+3\times10^{-17}$  & ---     & holds \\
        $0.49$   & $0.59990$ & $-4\times10^{-17}$  & ---     & holds \\
        \bottomrule
    \end{tabular}
\end{center}
Hypothesis (W) \emph{holds} precisely for $\nu>\nu_W\approx0.404$: there $\mu\ge0$ throughout, with only the forced
order-two zero at the clamped face, where $\tr\bm G\neq0$ (its normalised value decreasing from $0.23$ to $0.03$ as
$\nu\uparrow\tfrac12$). For $\nu<\nu_W$ the profile has $\mu<0$ on the clamped-side sector $(0,\theta_z(\nu))$, with
$\theta_z$ sweeping monotonically from the clamped face at $\nu_W$ to the free face as $\nu$ decreases (the whole
opening is negative below $\nu\approx0.08$); $\tr\bm G\neq0$ at every zero across the range, so the \emph{layer}
structure of Lemma~\ref{lem:screen} is as assumed --- it is the \emph{sign} clause that fails. Since the deep-field
orientation $J\sim K^2r^{2(\alpha-1)}\mu$ is independent of the load sign, $\mu<0$ on a sector means the linear
ansatz is orientation-reversing at depth there for \emph{either} loading: below $\nu_W$ the screened-vertex
conclusion of Theorem~\ref{thm:A}(A) is established only on the free-face sector, and on the clamped-side sector the
inner state must leave the linear ansatz below the crossover scale (the self-consistency caveat of
Remark~\ref{rem:Wstatus}). The intermediate-annulus statements, which is all the reduction of
Proposition~\ref{prop:blowup} uses, are unaffected. The failure is soft near the threshold ($|\min\mu|$ falls from
$2\times10^{-2}$ at $\nu=0.05$ to $10^{-7}$ at $\nu=0.40$) and absent in the near-incompressible range where the
compressive computations of this section operate.

\subsection{The buckling threshold \texorpdfstring{$\lambda_\star(\nu)$}{lambda*(nu)} and condition (BC)}
\label{sub:numbiot}

By Lemma~\ref{lem:bcwedge} condition (BC) reduces to a comparison of scale-invariant wedge thresholds and is set, on the
loading side, by the surface-instability stretch $\lambda_\star$ of the compressed half-space (Assumption~\ref{ass:sc},
Lemma~\ref{lem:biot}). For the compressible neo-Hookean energy \eqref{eq:neohooke} this is computed from the incremental
moduli about the homogeneous base state $\bm{F}_0=\mathrm{diag}(\lambda_\parallel,\lambda_\perp)$ with the free surface
in self-equilibrium, $\bm{P}_0\bm{N}=\bm0$. A surface mode $\bm{u}=\bm{U}\,e^{\mathrm{i}kx_1+sk x_2}$ decaying into the
body ($\Re s>0$) exists when the $2\times2$ incremental-traction determinant vanishes; since the modulus is constant in
the homogeneous base state the four roots $s$ follow from a characteristic quartic (a Stroh problem), and the secular
condition is independent of the wavenumber $k$ --- the scale-free signature of creasing exploited in the limit
$k\to\infty$ of Lemma~\ref{lem:biot}.

The solver reproduces the classical incompressible Biot value: as $\lambda_e/\mu_e\to\infty$ (the incompressible limit,
$\nu\to\tfrac12$) the threshold tends to $\lambda_\star=0.5437$. For the compressible model it furnishes
$\lambda_\star$ as a function of the material ratio, e.g.
\begin{center}
    \begin{tabular}{rcc}
        \toprule
        $\lambda_e/\mu_e$ & $\nu$ & $\lambda_\star$ \\
        \midrule
        $1$    & $0.250$ & $0.514$ \\
        $5$    & $0.417$ & $0.536$ \\
        $50$   & $0.490$ & $0.543$ \\
        $\infty$ & $0.5$ & $0.5437$ \\
        \bottomrule
    \end{tabular}
\end{center}
What this computes is the \emph{stretch} threshold $\lambda_\star(\nu)$, which is the amplitude-free, $\nu$-only wedge
datum on the buckling side of (BC) (Lemma~\ref{lem:bcwedge}); the buckling load $t_c\le t_*$ is then attained while the
fundamental stretch is still $\lambda_\parallel=\lambda_\star=\mathcal{O}(1)$, away from collapse. We stress that
$\lambda_\star$ is a stretch, not a load: converting it to the corner load $t_*$ requires the global fundamental-state
map $t\mapsto\lambda_\parallel(\bm F_0(t);P)$, which we do not compute here and which is monotone under the assumed
loading. The threshold thus settles the ordering $t_c<t_{\mathrm{coll}}$ only together with that monotone map and with a
lower bound on the competing collapse load $t_{\mathrm{coll}}$, which the barrier keeps high but whose quantitative tip
bound is left open (Lemma~\ref{lem:bcwedge}, Remark~\ref{rem:bc}). The computation is reproduced by
\texttt{surface\_instability.py}, validated against the incompressible Biot threshold.

\subsection{The one remaining solve: the subcritical sign \texorpdfstring{$b(\nu,\hat K)$}{b(nu,Khat)}}
\label{sub:numb}

The two preceding quantities are essentially analytic. The single genuinely numerical residue is the sign of the reduced
cubic coefficient (Assumption~\ref{ass:subcrit}), which by Proposition~\ref{prop:bcount} does \emph{not} collapse to a
universal number --- with $\alpha>\tfrac12$ the cubic integral is tip-convergent but not tip-dominated, so $b=b(\nu,\hat
K)$ retains the stress-intensity dependence. Its evaluation is a two-dimensional transverse solve on $\mathcal{L}_W$: the
critical buckling mode $\bm{\phi}$, the second-order Lyapunov--Schmidt field $\bm{\psi}$ solving $Q\mathcal{L}_cQ\,\bm{\psi}
= -\tfrac12 Q\,\partial^2 G[\bm{\phi},\bm{\phi}]$ on $V^\perp$, and the cubic integral $b=b_{\mathrm{dir}}+b_\psi$.

We note for implementation that this last solve is the one place a plain Fourier discretisation does \emph{not} suffice:
in two dimensions the crease oscillates along the free face in the same coordinate in which the corner pre-stress is
graded, so the surface direction is not translation-invariant (the two-scale competition of
Remark~\ref{rem:twoscales}). The appropriate discretisation is again Mellin\,$\times$\,Chebyshev --- Chebyshev in
$\theta$, mapped-spectral in $\log r$ --- with the edge handled by the analytic Fourier reduction of
Proposition~\ref{prop:blowup}(iii) in three dimensions. The buckling eigenproblem and the surface mechanism are exactly
the two ingredients validated in \S\ref{sub:numpencil}--\S\ref{sub:numbiot}; assembling them over the
$(\nu,\hat K)$ rectangle produces the phase diagram whose sign is the last open condition. No finite-element model enters
the chain: Mellin resolves the corner, Fourier the edge, and a spectral depth discretisation the buckling.

We stress that this graded solve is genuinely necessary, not a refinement: by Remark~\ref{rem:flatdegenerate} the
homogeneous (flat) reduction is degenerate --- the second-harmonic response is resonant and $b$ is undefined --- so the
grading is what regularises the coefficient.

% Surrogate-cell computation: first evidence, not the prescribed wedge solve.
A first version of this solve has been carried out on a \emph{surrogate} of the wedge: a graded half-space cell,
discretised by a single \emph{consistent} weak (Galerkin) scheme --- Chebyshev along the free face $X$, mapped
Chebyshev in depth $Y$ --- with the corner pre-stress mimicked by a \emph{linear} monotone grading
$\lambda_\parallel(X)=\bar\lambda(1+\hat K\,X/L)$ through the Biot threshold, in place of the physical
$r^{\alpha-1}$ profile; the Mellin\,$\times$\,Chebyshev wedge solve prescribed above has not yet been performed, and
the results below should be read as evidence from that proxy. The critical mode $\bm\phi$ is taken as the kernel of the
\emph{same} weak operator that is then inverted in the Lyapunov--Schmidt step, and the second harmonic is carried in
real nodal degrees of freedom; this removes the inconsistency that made an earlier flat single-mode attempt diverge.
Three facts emerge:
\begin{itemize}
  \item[\textup{(i)}] The homogeneous ($\hat K=0$) cell carries \emph{no} discrete critical mode --- at $\lambda_\star$
  the operator is already indefinite, the scale-free signature of Remark~\ref{rem:flatdegenerate}.
  \item[\textup{(ii)}] The monotone grading produces a single \emph{localised} neutral mode with a discrete spectral
  gap to the rest of the spectrum ($\approx 4$--$5\times10^{-2}$ across $\hat K\in[0.05,0.30]$ at the working
  resolution), so on the discrete cell the second-order problem $Q\mathcal L_cQ$ is non-resonant and $\bm\psi$ ---
  and hence $b$ --- is well defined. The gap, however, \emph{decreases} under refinement (table below), so the
  isolation of the kernel in the continuum limit --- and with it the convergence of the reduced coefficients --- is
  not established by this computation.
  \item[\textup{(iii)}] Decomposing $b=b_{\mathrm{E_4}}+b_{\mathrm{fb}}$ into the (adverse, positive) frozen quartic and
  the Lyapunov--Schmidt feedback, the \emph{normalisation-invariant} ratio $b_{\mathrm{fb}}/b_{\mathrm{E_4}}$ --- both
  terms scale as $\|\bm\phi\|^4$, so the ratio is independent of the arbitrary mode amplitude --- lies between
  $-1.9$ and $-2.5$ at every tested resolution: below $-1$ in each case, though oscillating rather than settling to
  a converged value. At the tested resolutions the feedback \emph{overwhelms} the adverse frozen term, giving $b<0$.
\end{itemize}
The status of the two terms is asymmetric, and the computation should be read accordingly: $b_{\mathrm{E_4}}>0$ and
$b_{\mathrm{fb}}\le0$ are \emph{theorems} (Lemma~\ref{lem:volcoeff}(ii) and Lemma~\ref{lem:fbsign} --- the solver
cannot find, and never finds, a positive feedback), so the tables below test only the \emph{magnitude} criterion
$r<-1$. The diagnostic ratio $r:=b_{\mathrm{fb}}/b_{\mathrm{E_4}}$ is \emph{normalisation-invariant} (both terms scale
as $\|\bm\phi\|^4$) and decides the sign by $b<0\iff r<-1$. Its behaviour under refinement of the angular ($N$) and
depth ($N_X$) resolutions is shown below; $r$ stays below $-1$ throughout but does not settle, and the discrete
spectral gap shrinks steadily:
\begin{center}
    \begin{tabular}{rrcccc}
        \toprule
        $N_X$ & $N$ & ratio $r$ & sign$(b)$ & spectral gap \\
        \midrule
        $10$ & $24$ & $-2.092$ & $b<0$ & $5.8\times10^{-2}$ \\
        $12$ & $28$ & $-2.452$ & $b<0$ & $4.5\times10^{-2}$ \\
        $14$ & $32$ & $-2.426$ & $b<0$ & $3.5\times10^{-2}$ \\
        $16$ & $36$ & $-2.212$ & $b<0$ & $2.8\times10^{-2}$ \\
        \bottomrule
    \end{tabular}
\end{center}
(at $\nu=0.417$, $\hat K=0.15$; $r$ stays below $-1$ at every resolution but oscillates between $-2.09$ and $-2.45$,
and the concurrent decay of the gap warns that the surrogate cell may lack a well-defined continuum limit). Sweeping
the physical rectangle at fixed resolution, the ratio is below $-1$ --- hence $b<0$ --- throughout $(\nu,\hat K)$:
\begin{center}
    \begin{tabular}{rccc}
        \toprule
        $\nu\backslash\hat K$ & $0.10$ & $0.20$ & $0.30$ \\
        \midrule
        $0.250$ & $-3.19$ & $-2.43$ & $-2.44$ \\
        $0.333$ & $-2.70$ & $-2.14$ & $-2.37$ \\
        $0.417$ & $-2.17$ & $-1.86$ & $-2.19$ \\
        $0.455$ & $-1.82$ & $-1.65$ & $-1.94$ \\
        \bottomrule
    \end{tabular}
\end{center}
The margin to the threshold $r=-1$ is smallest near the incompressible corner ($\nu\to\tfrac12$, $\hat K\approx0.2$),
where $r\approx-1.65$, and is never breached on the physical rectangle. On this evidence the cubic clause of
Assumption~\ref{ass:subcrit} holds and the corner behaves as the genuine crease-analogue; but the evidence comes
from the surrogate cell and has not converged, so we treat it as strong support rather than verification. The
quintic clause $d>0$ is not tested by this computation at all and remains fully open (Remark~\ref{rem:fbstatus}).

% The Gamma certificate has now been run: it certifies b<0 on the rectangle without the bordered solve, validates the
% bordered ratios independently, and returns an honest negative on the certdeg hypotheses, which this cell cannot probe.
The inversion-free certification criterion of Corollary~\ref{cor:cert} has now been evaluated on the same
discretisation (\texttt{gamma\_certificate.py}), and it upgrades the table above in two ways. First, since the
assembled load vector represents the quadratic self-coupling, every conjugate-gradient iterate of it is an
\emph{explicit} test field --- a polynomial in the operator applied to the source, no bordered matrix, no
near-singular solve --- and by Krylov optimality the certified value $\Gamma_k$ of \eqref{eq:gammadef} increases
monotonically in $k$, each $k$ yielding the rigorous bound $b\le b_{\mathrm{dir}}(1-\Gamma_k)$. On the full rectangle
the certificate fires at between $66$ and $184$ iterations, reaching $\Gamma\in[1.31,3.01]$ by $k=1000$:
\emph{the sign $b<0$ of the discrete cell is certified at every tested $(\nu,\hat K)$ without inverting the reduced
operator}. Second, at the reference point the deep iteration converges to the bordered-solve value itself
($\Gamma_k\to2.12$ against $-r=2.17$ at $k=2000$), independently validating the assembled ratios of the table against
their own $\mathrm{cond}(B)\sim10^{7}$--$10^{8}$ pathology. Under refinement the certificate survives --- the
crossing iteration grows ($127\to324$ from the coarsest to the finest grid) as the shrinking gap slows the Krylov
convergence, but $\Gamma$ still exceeds $1.8$ at depth --- with the standing caveat that all of this certifies the
\emph{discrete} $b$ of the surrogate cell, at each resolution, and cannot repair the unsettled continuum limit noted
above. One honest negative completes the picture: the flat-degeneracy amplification mechanism of
Proposition~\ref{prop:certdeg} is \emph{not operative on this cell} --- the excited mode never goes neutral as
$\hat K\to0$ (its Rayleigh quotient stays near $0.09$, precisely because the monotone grading was designed to lift
the flat resonance) and its coupling to the source is small and erratic --- so hypothesis (b) of that proposition
remains untested; probing it requires the periodic or genuine-wedge geometry in which the doubled-wavenumber
harmonic exists. The $b$-computation itself is reproduced by \texttt{graded\_buckling.py}, which reuses the validated
linear building blocks of \S\ref{sub:numpencil}--\S\ref{sub:numbiot} (\texttt{williams\_clampedfree.py} /
\texttt{mellin\_chebyshev\_pencil.py} for $\alpha$; \texttt{surface\_instability.py} for $\lambda_\star$).

As a byproduct, the same critical mode furnishes a partial check of Assumption~\ref{ass:rot}
(Remark~\ref{rem:rotnum}): the neutral co-energy $\langle\bm\phi,\mathcal L\bm\phi\rangle\approx10^{-8}$ confirms the
mode is a genuine kernel, and the dimensionless rotation defect $\|\delta J\|/\|\Grad\bm\phi\|\approx0.08$--$0.16$ (stable
under deepening compression) confirms it is nearly volume-preserving. The decomposition of \eqref{eq:secondvar} shows the
singular determinant term to be $6$--$10\times$ the $f''(\delta J)^2$ term, so the co-energy is bounded by cancellation
against the shear term, not by the $f''$ term alone --- the honest mechanism behind the relief estimate. This supports
the qualitative volume-preserving property; the precise asymptotic rate $\delta J=\mathcal{O}(J_0/\sqrt{|\log J_0|})$ as
$J_0\to0$ is not reached by the accessible base states and remains an assumption.
